{
  "name": "smart-scribe",
  "version": "1.0.0",
  "description": "AI-powered voice to text transcription CLI using Google Gemini",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "smart-scribe": "src/index.ts"
  },
  "scripts": {
    "build": "bun build --compile --minify src/index.ts --outfile dist/smart-scribe",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src",
    "format": "biome format --write src",
    "format:check": "biome format src",
    "check": "biome check src",
    "check:fix": "biome check --write src"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/bun": "^1.3.3",
    "@types/node": "^24.10.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@google/genai": "^1.30.0",
    "@iarna/toml": "^2.2.5",
    "chalk": "^5.6.2",
    "mime": "^4.1.0",
    "ora": "^9.0.0"
  }
}
